import styled from "styled-components";

export const StyledPropertyList = styled.ul`
    list-style: none;
    display: flex;
    gap: 1.25rem;
    padding:1.25rem;
    width: 100%;
    flex-wrap:wrap;
 `